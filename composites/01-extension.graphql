type http://localhost:3000 @loadModel(id: "$ATTEST_ID") {
  id: ID!
}

type CeramicDev @loadModel(id: "$DEVELOPER_ID") {
  attestations: [http://localhost:3000] @relationFrom(model: "http://localhost:3000", property: "attestedProfileId")
}